# Copy this file to .env and fill in your database credentials.
# For shared cloud DB (e.g. Supabase), get these from Settings -> Database.
# For local development, leave as defaults.

DB_NAME=unify
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
